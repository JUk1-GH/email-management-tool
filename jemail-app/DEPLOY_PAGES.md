# Cloudflare Pages 部署说明

这个前端现在已经支持和独立后端域名分离部署。

## 默认行为

- 默认请求当前站点的 `origin`
- 如果你需要手动指定接口地址，可以修改 `config.js`

## Pages 建议目录

直接把整个 `jemail-app/` 目录作为静态站点发布目录。

## 发布前检查

1. 确认 `config.js` 存在
2. 如果前后端分离部署，请把 `config.js` 中的 `API_BASE` 改成真实接口地址
3. 确认后端已经允许来自前端域名的跨域请求
